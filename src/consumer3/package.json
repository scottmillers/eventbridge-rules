{
  "name": "consumer3",
  "version": "1.0.0",
  "description": "Write Failed Transactions to the DynamoDB Failed Transaction Table",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Scott Miller",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.481.0",
    "@aws-sdk/lib-dynamodb": "^3.481.0"
  }
}
