{
  "name": "consumer1",
  "version": "1.0.0",
  "description": "Write Approved Transactions to the DynamoDB Approved Table",
  "main": "handler.js",
  "scripts": {
    "start" : "node run-local.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Scott Miller",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.481.0",
    "@aws-sdk/lib-dynamodb": "^3.481.0"
  }
}
